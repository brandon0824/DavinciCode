# Docker-only deployment settings. Review these values before committing to a repository.
# Initial admin web-login password only (minimum 7 characters).
ADMIN_PASSWORD=brandon
# Required absolute host directory mounted to /var/lib/postgresql in the container.
PG_DATA_DIR=/root/davinci_pg
