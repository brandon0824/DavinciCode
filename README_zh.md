# 达芬奇密码 (DaVinci Code) 在线多人桌游

这是一个支持实时多人的“达芬奇密码”在线桌游系统，采用 **Next.js 14 App Router + Tailwind CSS + Framer Motion + PostgreSQL** 全栈技术开发。系统不仅支持本地开发运行，还完美适配 Serverless 部署与 Docker 容器化一键部署（包含宿主机持久化数据挂载与内置轻量 PostgreSQL 服务）。

Language / 语言选择:
- [English Documentation](README.md)
- [中文文档](README_zh.md)

项目文档：
- [英文架构设计文档](doc/design.md)
- [中文架构设计文档](doc/design_zh.md)
- [优化计划](doc/IMPROVEMENT_PLAN.md)
- [运维手册](doc/OPERATIONS.md)

---

## ✨ 系统核心功能与特色

* **🔑 强制用户注册与安全认证 (bcrypt Authentication)**：
  * **登录门禁校验**：用户必须先完成账号注册/登录，方可创建房间或进入任何现有房间。系统在前端与后端 PostgreSQL 数据库双重校验用户合法身份。
  * **密码哈希存储**：新密码使用 bcrypt 哈希；旧版 Base64 记录在成功登录后自动升级为 bcrypt。密码长度至少需要 7 个字符。
  * **会话持久化**：登录状态自动持久化保存于浏览器 `sessionStorage`，刷新或重新打开网页无需重复登录。

* **👑 系统管理员账号与专属控制台 (`/admin`)**：
  * **预置管理员账号**：系统初始化时使用环境变量 `ADMIN_PASSWORD` 建立管理员账号，生产环境不得使用默认密码；
  * **全服胜率排行榜隔离**：全服胜率排行榜 (`/stats`) 自动过滤 `admin` 账号，榜单全为正规真实对战玩家；
  * **专属管理后台 (`/admin`)**：仅限 `admin` 浏览。以响应式图表与表格展示全服所有用户明细，包括：用户名、胜场/负场/总局数、胜率%、15 秒心跳在线状态（`🟢 在线 / ⚪ 离线`）、注册时间与最后在线时间。
  * **权限隔离防护**：`admin` 账号专用于全服数据管理，无建房与切局对战权限，试图建房或加入房间时系统自动拦截提示。

* **🃏 黑白双任意百搭牌 (`-` 牌) 与自由插牌规则**：
  * **26 张完整牌堆**：包含黑色 0-11（12 张）+ 白色 0-11（12 张），以及 **1 张黑色任意百搭牌 `-`** 和 **1 张白色任意百搭牌 `-`**。
  * **自由插入任意位置**：遵循《达芬奇密码》官方经典规则，当玩家摸到或手牌持有任意百搭牌 `-` 时，可**自由选择插入手牌的任意缝隙位置**（最左侧、卡牌之间或最右侧）混淆对手心理博弈；手牌区提供“`⇄ 位置`”微调功能。

* **🏆 个人战绩与全服胜率排行榜 (`/stats`)**：
  * 数据库在 `users` 表中永久保存每位玩家的对战数据（`total_games` 场次, `total_wins` 胜场, `total_losses` 负场），在 `match_history` 表中保存每局开赛时间与胜负明细。
  * `/stats` 战绩页面展示“个人历史对战明细”与“全服胜率排行榜”（榜首独享金色 🥇 尊贵外观），支持移动端自适应排版。

* **🎉 结算弹窗防自动跳转 & 全员手牌底牌公开**：
  * 游戏结束时自动显示胜利/对局完结弹窗，并**完全公开场上所有玩家的最终手牌数字与百搭牌**，供玩家复盘底牌。
  * 取消自动强制跳转，只有当玩家亲自点击 `[返回房间待命 (准备下一局)]` 按钮后，才会平滑切回房间等待大厅。

* **⏰ 30 秒倍数超时提醒机制**：
  * 轮到自己回合时触发独立计时器，当停留思考超过 30 秒、60 秒、90 秒... 时，自动弹窗与浮动 Toast 醒目提醒玩家做出选择。

* **🎨 现代化 Better-UI & 双主题切换**：
  * 基于 `better-ui` 设计工程规范，支持全局触觉式按压反馈 (`Scale on Press`)、同心圆角与双色（Light/Dark）主题自由切换。
  * 针对 iPhone / Android 移动端屏幕进行全方位适配，绝无文字重叠或竖排错位。

* **🟢 全局实时在线人数与 15 秒心跳检测**：
  * 页面底部显示 **`🟢 当前在线玩家：X 人`** 带有绿色脉冲指示灯的统计条；
  * 客户端每 4 秒静默发送心跳打卡，当用户关闭浏览器标签页后，15 秒内自动扣减离线人数，实现精准在线统计。

* **⚠️ 房间玩家 20 秒断线/关窗口自动踢出 & 动态日志**：
  * 房间内玩家无心跳超过 20 秒（如直接关闭 Chrome 窗口），系统自动将其移出房间并释放槽位；
  * 若离线的是房主，房主权限自动无缝转移给房间内剩余成员；
  * 房间等待大厅新增 **`📢 房间动态提示`** 消息流，实时同步玩家加入（`📢`）、主动离开（`🚪`）与掉线踢出（`⚠️`）。
  * 房间关闭后会立即删除该房间的全部聊天消息，包括最后一名玩家离开导致房间销毁的情况。

* **🔑 房主重返自建加密房间免输入密码**：
  * 房主若误退大厅重返自己创建的加密房间，前端免除密码弹窗，后端自动校验房主身份无缝重返房间。

* **🐳 Docker 一键部署与数据持久化**：
  * 提供 `docker-build.sh`（一键打包部署）与 `cleanDBAndRestart.sh`（一键清空数据重置）Shell 脚本；
  * Docker 宿主机挂载目录由 `.env.docker` 中必填的 `PG_DATA_DIR` 指定（示例为 `/root/davinci_pgdata`），用户账号与对战历史战绩数据**永久保存**。

* **🌐 外网远程数据库直连支持**：
  * 自动开放 PostgreSQL `5432` 端口与 `listen_addresses = '*'`，支持通过 Navicat / DBeaver / DataGrip 等外网数据库工具直连。

---

## 🛠️ 整体技术栈

* **核心框架**：Next.js 14 (App Router)
* **前端视图与动效**：React 18, Tailwind CSS, Framer Motion, Lucide React
* **后端 API**：Next.js Route Handlers (RESTful 无状态 API)
* **密码哈希**：bcrypt，兼容旧哈希自动迁移
* **数据库**：PostgreSQL (配合 `pg` 连接池与 JSONB 二进制大对象存储)
* **容器化**：Docker (内置 PostgreSQL 15 与自动化 Shell 部署脚本)

---

## 🔌 数据库外网直连信息 (Navicat / DBeaver / DataGrip)

在服务器开放安全组 `5432` 端口后，可通过以下信息直接连接容器内的数据库：

- **主机名/IP (Host)**: 你的 Linux 服务器外网 IPv4 地址
- **端口 (Port)**: `5432`
- **初始数据库 (Database)**: `davinci`
- **用户名 (Username)**：部署时配置的 `DB_USER` 数据库账号
- **密码 (Password)**：`brandon_pgdata`（Docker PostgreSQL `root` 账号固定密码）

---

## 🚀 快速开始

本项目支持 **Docker 一键镜像启动** 或 **本地开发环境启动**。

### 方式一：Docker 一键 Shell 脚本部署（推荐，端口 60824）

执行 Docker 脚本前，请在根目录创建 `.env.docker`，且只保留以下两个部署配置：

```env
ADMIN_PASSWORD=change-me-admin
PG_DATA_DIR=/root/davinci_pgdata
```

`ADMIN_PASSWORD` 仅用于网页管理员登录。Docker PostgreSQL 使用固定连接信息：用户名 `root`，密码 `brandon_pgdata`。

Docker 内 PostgreSQL 默认监听所有网络接口，脚本会将容器 `5432` 端口映射到宿主机。请通过服务器防火墙或云安全组限制访问来源。

我们在 Docker 镜像中封装了 Next.js 运行环境与内置 PostgreSQL 数据库：

1. **一键运行部署脚本**：
   ```bash
   chmod +x docker-build.sh && ./docker-build.sh
   ```
2. **重置/清空数据库并重新部署脚本**（可选）：
   ```bash
   chmod +x cleanDBAndRestart.sh && ./cleanDBAndRestart.sh
   ```
3. **访问游戏**：
   打开浏览器访问 `http://<服务器IP>:60824` 即可开始游玩！

---

### 方式二：本地 Node.js 开发环境运行

需准备 [Node.js (22+)](https://nodejs.org) 和运行中的 [PostgreSQL (14+)](https://www.postgresql.org)。

#### 1. 安装项目依赖
```bash
npm install
```

#### 2. 配置环境变量
启动前请直接配置根目录 `.env.local`。本地 Node.js 启动从 `.env.local` 读取数据库和管理员配置，不使用 `PG_DATA_DIR`。
```env
DB_HOST=localhost
DB_PORT=5432
DB_USER=root
DB_PASSWORD=local-password
DB_NAME=davinci
ADMIN_PASSWORD=local-admin-password
```

#### 3. 运行数据库迁移脚本
初始化 `davinci` 数据库及所需的 `users` / `rooms` 表结构：
```bash
node scripts/setup-pg.js
```
如果 `DB_NAME` 已存在，脚本会先校验所需表和字段；发现结构不符合要求时会直接报错停止，不会静默修改已有数据库。

#### 4. 启动开发服务器
```bash
npm run dev
```
也可以直接执行脚本完成本地启动的全部步骤：
```bash
./localStart.sh
```
启动后访问 `http://localhost:60824`。

#### 5. 生产构建打包
```bash
npm run build
npm start
```

---

## 🎮 游戏规则与对局流程

1. **注册与登录账号**：
   * 首次进入必须在主页注册或登录账号，未登录用户无法创建或进入任何房间。
2. **创建或加入房间**：
   * 已登录用户可创建房间（支持自定义房间号与密码）或直接点击“可用房间列表”中的房间加入。
3. **等待与开赛**：
   * 房间内满 2-4 人时，房主界面会醒目提示并展示“开赛”按钮，点击即可发起对局。
4. **回合行动逻辑**：
   * **摸牌**：轮到行动的玩家首先从黑色或白色牌堆中摸一张牌，普通牌按数字升序（黑左白右）排列，任意百搭牌（`-` 牌）显示在右侧。
   * **猜牌**：点击任意对手的一张暗牌，选择猜测数字 `0`-`11` 或 `- (任意百搭牌)`。
     - **猜对**：对手该牌被公开。可以选择继续猜测别的隐藏牌，或点击 Pass 结束回合。
     - **猜错**：作为惩罚，自己本轮新摸的牌将被强制公开，且本轮回合结束。
   * **认输与出局**：手牌全部公开的玩家宣告出局；玩家亦可主动选择“认输”。生存到最后的唯一玩家获得胜利。
5. **对局结算与复盘**：
   * 胜利/完结弹窗中全量公开所有玩家的最终手牌底牌，确认无误后点击 `[返回房间待命 (准备下一局)]` 回到等待大厅。
